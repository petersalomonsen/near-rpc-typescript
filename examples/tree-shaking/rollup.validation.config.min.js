import resolve from '@rollup/plugin-node-resolve';
import typescript from '@rollup/plugin-typescript';
import terser from '@rollup/plugin-terser';

export default {
  input: 'main-with-validation.ts',
  output: {
    file: 'dist/bundle-with-validation.min.js',
    format: 'es',
    sourcemap: false,
  },
  plugins: [
    resolve({
      preferBuiltins: false,
      browser: true,
    }),
    typescript({
      tsconfig: './tsconfig.json',
    }),
    terser({
      compress: {
        drop_console: false, // Keep console.log for demonstration
        drop_debugger: true,
        pure_funcs: [],
      },
      format: {
        comments: false,
      },
    }),
  ],
  external: [],
};
